services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: recheck
      POSTGRES_PASSWORD: recheck
      POSTGRES_DB: recheck
    ports:
      - "5432:5432"
    volumes:
      - recheck_pg:/var/lib/postgresql/data

volumes:
  recheck_pg:
